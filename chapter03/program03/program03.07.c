#include <stdio.h>
#include <stdbool.h>

int main(void) {
  bool a, b, c;
  int in = 1024;

  a = true;
  b = false;
  c = in + a + c;

  printf("a = %d, b = %d, c = %d\n", a, b, c);

  return 0;
  }
